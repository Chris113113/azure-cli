apiVersion: v1
kind: Service
metadata:
  name: sparkbatch-service
  labels:
    app: sparkbatch
spec:
  type: LoadBalancer
  ports:
  - port: 8001
    protocol: TCP
    targetPort: 8080
  selector:
    run: azureml-fe